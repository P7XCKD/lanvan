# Android/Termux Specific Requirements for Lanvan
# Core dependencies with Android/Termux compatibility

# Essential packages
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
jinja2>=3.1.2
python-multipart>=0.0.6

# Android/Termux optimized packages (No Pillow/PIL to avoid build compilation failures)
qrcode>=7.4.2
# pillow>=10.0.0 (Excluded to prevent build isolation errors on Android)
aiofiles>=23.2.0

# Network and system
zeroconf>=0.132.2
psutil>=5.9.6

# Security
cryptography>=41.0.7

# WebSocket support (essential for real-time features)
websockets>=11.0.2
wsproto>=1.2.0

# Android-specific optimizations
uvloop>=0.19.0  # Recommended for 2-4x loop speed increase on Termux
# pyperclip  # May not work on Android
# aiortc     # P2P features may be limited on mobile